info:
  name: Reset password
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/users/reset-password
  body:
    type: json
    data: |-
      {
        "name": "Ramiro",
        "email": "rami@test.com"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true
